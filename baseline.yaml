# Parameters for the baseline experiment.

experiment_name: baseline
task: "Summarize recent AI research papers"
num_trials: 5

agent:
  model: synthetic
  max_steps: 8
  use_tool_at_step: 2
  fail_at_step: null

pricing:
  prompt_price_per_1k: 0.005
  completion_price_per_1k: 0.015